#include "imagefile.h"

namespace libimage {

const libbase::vcs imagefile::version("Image File module (imagefile)", 1.00);

imagefile::imagefile()
   {
   }

imagefile::~imagefile()
   {
   }

} // end namespace
